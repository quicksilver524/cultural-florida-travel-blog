{{!-- <article class="post">
  <div class="title">
    <a href="/post/{{id}}">{{title}}</a><br />
    <img src="{{cover_url}}" />
    <br />
    <p>{{{post_body}}}</p>
  </div>
  <div class="meta">
    Created by
    {{user.username}}
    on
    {{format_date createdAt}}
    |
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>| vote{{vote_count}}
    time(s)
  </div>
</article> --}}
<div class="row">
  <div class="col s12 m7">
    <div class="card">
      <div class="card-image">
        <img src="{{cover_url}}" />
        <a href="/post/{{id}}">{{title}}</a>
      </div>
      <div class="card-content">
        <p>{{{post_body}}}</p>
      </div>
      <div class="card-action meta">
        Created by
        {{user.username}}
        on
        {{format_date createdAt}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>| vote
        {{vote_count}}
      </div>
    </div>
  </div>
</div>